name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch: # allows manual trigger from GitHub UI

permissions:
  contents: write # needed to push to gh-pages branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Export web build
        run: npx expo export -p web
        env:
          NODE_ENV: production

      - name: Fix asset paths for GitHub Pages sub-path
        run: |
          # Expo Metro emits absolute paths (e.g. /_expo/...) which break under /CryptApp/.
          # Replace every leading "/" reference with "/CryptApp/" in the generated index.html.
          sed -i 's|src="/|src="/CryptApp/|g; s|href="/|href="/CryptApp/|g' dist/index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'Deploy CryptVault to GitHub Pages'
